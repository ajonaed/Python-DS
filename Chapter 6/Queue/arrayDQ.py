''' Double ended queue where add and remove action works on both end. Function Name:
add_first, add_last, delete_first, delete_last, first(), last(). These function perform
work as the name described. Page 249 '''